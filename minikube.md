### create a minikube multinode cluster 
``` minikube start --nodes 2 -p multinode-demo ```

### create a minikube multinode cluster with auto-pause 
``` minikube start --nodes 2 -p multinode-demo addons enable auto-pause``` 
